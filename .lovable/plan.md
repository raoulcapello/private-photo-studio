

## Update Favicon

### Steps

1. Copy the uploaded image `user-uploads://favicon-2.png` to `public/favicon.png` (overwriting the existing favicon).
2. No changes needed in `index.html` — it already references `/favicon.png`.

### Files to modify

| Action | Path |
|--------|------|
| Copy (overwrite) | `user-uploads://favicon-2.png` → `public/favicon.png` |

